<h1>Seu Carrinho</h1>

{{#if carrinho.length}}
  <ul>
    {{#each carrinho}}
      <li>
        {{nome}} - {{quantidade}} x R$ {{preco}} = R$ {{multiply preco quantidade}}
        <form action="/carrinho/remover/{{idProduto}}" method="POST" style="display:inline;">
          <button type="submit">Remover</button>
        </form>
      </li>
    {{/each}}
  </ul>

  <p><strong>Total:</strong> R$ {{total}}</p>

  <a href="/checkout">Finalizar Compra</a>
{{else}}
  <p>Seu carrinho está vazio.</p>
{{/if}}
