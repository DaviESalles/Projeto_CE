<!DOCTYPE html>
<html>
<head>
    <title>E-commerce da Agricultura Familiar</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>​
</head>
<body>
    <center>
        <br>
        <h1> E-commerce da Agricultura Familiar </h1>

        {{#if vendedor}}
            <label>Usuário Logado: Vendedor ({{vendedorLogin}})</label>
        {{else if cliente}}
            <label>Usuário Logado: Cliente ({{clienteLogin}})</label>
        {{else}}
            <label>Visitante</label>
        {{/if}}

        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="/home">Home</a>
                    </li>

                    {{#if cliente}}
                    <li class="nav-item">
                        <a class="nav-link" href="/carrinho">Carrinho</a>
                    </li>
                    {{/if}}
{{#if admin}}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownClientes" role="button"
                            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Clientes
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownClientes">
                            
                                <a class="dropdown-item" href="/clienteCreate">Criar</a>
                            
                            <a class="dropdown-item" href="/clienteList">Listar</a>
                        </div>
                    </li>{{/if}}

                    {{#if admin}}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownVendedores" role="button"
                            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Vendedores
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownVendedores">
                            <a class="dropdown-item" href="/vendedorCreate">Criar</a>
                            <a class="dropdown-item" href="/vendedorList">Listar</a>
                        </div>
                    </li>
                    {{/if}}

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownCategorias" role="button"
                            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Categorias
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownCategorias">
                            {{#if admin}}
                                <a class="dropdown-item" href="/categoriaCreate">Criar</a>
                            {{/if}}
                            <a class="dropdown-item" href="/categoriaList">Listar</a>
                        </div>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownProdutos" role="button"
                            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Produtos
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownProdutos">
                            {{#if admin}}
                                <a class="dropdown-item" href="/produtoCreate">Criar</a>
                            {{/if}}
                            {{#if vendedor}}
                                <a class="dropdown-item" href="/produtoList">Meus Produtos</a>
                            {{else}}
                                <a class="dropdown-item" href="/produtoList">Listar</a>
                            {{/if}}
                        </div>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownPedidos" role="button"
                            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Pedidos
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownPedidos">
                            <a class="dropdown-item" href="/pedidoCreate">Criar</a>
                            <a class="dropdown-item" href="/pedidoList">Listar</a>
                        </div>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </nav>​

        <br>
        {{{body}}}
    </center>
</body>
</html>
